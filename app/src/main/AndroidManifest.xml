<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xcp.designpatterns">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp_proxy.sample.MVPProxyActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".activity.PrincipleActivity" />
        <activity android:name=".mvc.MVCActivity" />
        <activity android:name=".mvp.MVPActivity" />
        <activity android:name=".mvvm.MVVMActivity" />
        <activity android:name=".observerpatterns.RegisterActivity" />
        <activity android:name=".observerpatterns.InfoListActivity">
        </activity>
    </application>

</manifest>